{
  "name": "@act-spec/_test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Internal test helpers (fixture loaders, reporter assertions). Not published.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "conformance": "node -e \"console.log('no conformance gate for @act-spec/_test-utils (internal)')\""
  },
  "dependencies": {
    "@act-spec/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
