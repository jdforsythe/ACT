{
  "id": "extract-throws",
  "requirements": ["PRD-301-R16", "PRD-300-R22"],
  "description": "An extract function that throws produces a marketing:placeholder block in the canonical draft, with metadata.extraction_status=failed; render continues past the failure so descendants still contribute.",
  "expectedBlockTypes": ["marketing:placeholder", "marketing:pricing-table"],
  "expectedFirstBlockMetadata": {
    "extraction_status": "failed",
    "extracted_via": "component-contract"
  }
}
