{
  "id": "error-message-with-secret",
  "requirements": ["PRD-302-R19", "PRD-300-R32"],
  "description": "An extract function that throws an error whose message contains a string matching the v0.1 secret-pattern set (here: sk_live_*) MUST have the matching substring redacted to [REDACTED] before the binding emits the placeholder. The binding MUST NOT include raw stack traces in metadata.error; only the truncated, redacted Error.message.",
  "expectedFirstBlockType": "marketing:placeholder",
  "expectedErrorContains": "[REDACTED]",
  "expectedErrorNotContains": "sk_live_ABCDEFG123"
}
