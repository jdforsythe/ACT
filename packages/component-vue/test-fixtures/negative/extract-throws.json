{
  "id": "extract-throws",
  "requirements": ["PRD-302-R16", "PRD-300-R22"],
  "description": "An extract function that throws produces a marketing:placeholder block in the canonical draft, with metadata.extraction_status=failed and metadata.extracted_via=component-contract. Render continues past the failure so descendants still contribute their contracts (Survivor still appears in the content list).",
  "expectedBlockTypesContain": ["marketing:placeholder", "marketing:pricing-table"],
  "expectedFirstBlockMetadata": {
    "extraction_status": "failed",
    "extracted_via": "component-contract"
  }
}
