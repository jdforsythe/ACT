---
id: api-reference
title: API Reference
type: reference
tags: [api]
---

The Acme HTTP API exposes resources for projects, deployments, and tokens. All
endpoints are versioned under `/v1/` and require a bearer token.

## Endpoints

See the OpenAPI spec for the canonical surface; the table here mirrors the
top-level resource tree.
