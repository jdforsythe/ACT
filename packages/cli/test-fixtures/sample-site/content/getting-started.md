---
title: Getting started
summary: A short walkthrough of what the CLI fixture demonstrates.
type: guide
parent: index
---

The CLI loads `act.config.mjs`, instantiates the markdown adapter against the
`content/` directory, and writes the static file set under `dist/`.
