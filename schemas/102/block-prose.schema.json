{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://act-spec.org/schemas/0.1/block-prose.schema.json",
  "title": "ACT content block — prose",
  "description": "Per PRD-102-R2. Plain prose with no markdown formatting (default), or explicit format declaration. Conformance: Standard.",
  "type": "object",
  "required": ["type", "text"],
  "additionalProperties": true,
  "properties": {
    "type": {
      "type": "string",
      "const": "prose"
    },
    "text": {
      "type": "string",
      "description": "Plain prose. Whitespace and newlines preserved verbatim."
    },
    "format": {
      "type": "string",
      "default": "plain",
      "description": "Documented-open enum. Well-known values: `plain` (default). Adding values is MINOR per PRD-108-R4(3).",
      "examples": ["plain"]
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
