name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  ci:
    name: Node ${{ matrix.node }}
    strategy:
      fail-fast: false
      matrix:
        node: ['20', '22']
    uses: ./.github/workflows/_package.yml
    with:
      node-version: ${{ matrix.node }}
